/*BREAKPOINTS 
Normal reakpoints - Where the program stops when the corresponding line of code is reached.

Other types of breakpoints
* Conditional breakpoints : Allows the program to stop at a particular line if the condition at that line is met(true)

*DOM breakpoints: Allow the program to stop when the web page has been dynamically modified.

*Even listener breaker: Allow the execution to stop when a specific event is triggered.
*XRH breakpoint: Allows the execution to stop when an ajax call is made.


The chrome developer tools is used for debbugging.
*/

