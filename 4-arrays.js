/* ARRAYS
Arrays in Javascript can be made up different types of data. This is due to the fact that Javascript is losely typed.
Arrays have dynamic size therefore the length can be changed at any time.

syntax:
Arrays can be declared by using array literal notation

variable name = [array elements];
*/

let foods = ["banku", "rice", "gari", 20, true];
console.log(foods); //array object with elements included ['banku', 'rice', ...]
